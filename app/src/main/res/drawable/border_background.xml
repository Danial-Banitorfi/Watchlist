<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <!-- Das Haupt-Design des Buttons -->
    <item>
        <shape android:shape="rectangle">
            <!-- Hintergrundfarbe -->
            <solid android:color="@android:color/transparent" />

            <!-- Die Umrandung -->
            <stroke
                android:width="3dp"
                android:color="@color/grey" />

            <!-- Abgerundete Ecken -->
            <corners android:radius="8dp" />

            <!-- Innenabstand (Padding) innerhalb des Rahmens -->
            <padding
                android:bottom="4dp"
                android:left="17dp"
                android:right="17dp"
                android:top="4dp" />
        </shape>
    </item>

    <!-- Die Maske sorgt dafür, dass der Ripple-Effekt nur innerhalb der abgerundeten Ecken leuchtet -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="8dp" />
        </shape>
    </item>

</ripple>